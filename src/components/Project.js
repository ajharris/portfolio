//Project will be the container item in a list of Projects
import { useState } from "react";

export default function Project (){

    const [webAddress, setWebAddress] = useState('')
    const [githubAddress, setGithubAddress] = useState('')

    return null;
}